<div>
	<h2>Agregar Cliente</h2>
	<% form_for(:cliente, :url => {:action => "crear", :id => @cliente.id} ) do |c| %>
	<%= render(:partial => "form", :locals => {:c => c}) %>
	<div>
		<%= submit_tag("Agregar") %>
	</div>
	<%end%>
</div>
